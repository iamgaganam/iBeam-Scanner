name: ibeacon_wrapper
description: Abstraction layer for iBeacon scanning and proximity estimation.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.2.1
  permission_handler: ^12.0.1

dev_dependencies:
  lints: ^6.0.0

flutter:
